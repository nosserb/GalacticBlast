/* ==============================
   Déclaration de la police locale
   ============================== */
@font-face {
  font-family: 'NeonPhantom';
  src: url('./fonts/NeonPhantom.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
}

/* ==============================
   Styles globaux
   ============================== */
body {
  margin: 0;
  padding: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #0f0c29; /* Fond sombre */
  font-family: 'NeonPhantom', Arial, sans-serif;
}

/* ==============================
   Styles pour le SVG
   ============================== */
svg {
  width: 80%;
  max-width: 800px;
  height: auto;
  display: block;
}

svg rect {
  fill: url(#gradient);  /* Dégradé bleu/violet */
  rx: 20;                /* Coins arrondis */
  ry: 20;
}

svg text {
  font-family: 'NeonPhantom', Arial, sans-serif;
  font-size: 48px;
  fill: white;
  text-anchor: middle;
  dominant-baseline: middle;
  letter-spacing: 1.5px;
  text-shadow: 2px 2px 5px rgba(0,0,0,0.5);
}
