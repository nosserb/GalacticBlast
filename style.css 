/* ==============================
   Déclaration de la police locale
   ============================== */
@font-face {
  font-family: 'HIGHSPEED';
  src: url('./fonts/HIGHSPEED.TTF') format('truetype');
  font-weight: normal;
  font-style: normal;
}

/* ==============================
   Styles globaux
   ============================== */
body {
  margin: 0;
  padding: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #0f0c29; /* Fond sombre autour */
  font-family: 'HIGHSPEED', Arial, sans-serif;
}

svg {
  width: 80%;
  max-width: 800px;
  height: auto;
  display: block;
}

svg rect {
  fill: #0f0c29; /* Fond sombre du rectangle */
  rx: 20;        /* Coins arrondis */
  ry: 20;
}

svg text {
  font-family: 'HIGHSPEED', Arial, sans-serif;
  font-size: 60px;
  text-anchor: middle;
  dominant-baseline: middle;
  fill: rgba(173, 216, 230, 0.8); /* Bleu clair légèrement transparent */
  filter: drop-shadow(0 0 10px rgba(173,216,230,0.7))
          drop-shadow(0 0 20px rgba(173,216,230,0.5)); /* Glow lumineux */
  letter-spacing: 1px;
}
